name: "run-bash"

on: 
   workflow_dispatch:

jobs:
  run-bash:
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: run bash
        run: |
          sudo bash ./bash.dl.sh
